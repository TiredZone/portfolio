// Analytics utilities - GA4 and Cloudflare event tracking
export const trackEvent = (
    eventName: string,
    params?: Record<string, unknown>
) => {
    // TODO: Implement tracking
    console.log("Track event:", eventName, params);
};
